{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the tine desktop shell. Grants the main window access to the core APIs plus the dialog and shell plugins used by the embedded IPC commands (pick_project_folder, save_export_file, shell.open).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:allow-open"
  ]
}
